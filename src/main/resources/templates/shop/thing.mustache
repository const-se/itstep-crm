<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8"/>
    <title>Товар</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css"/>
</head>
<body>
<div class="container p-5">
    <h1>{{thing.name}}</h1>
    <ul>
        <h1>{{thing.brand.name}} {{thing.name}}</h1>
        <a href="/category/{{thing.category.id}}">{{thing.category.name}}</a>
        <br/>
        Цвет:
        <span style="display: inline-block;
                     border: 2px solid black;
                     background-color: #{{thing.color}};
                     height: 20px;
                     width: 20px;"></span>
        <br/>
        Размер: {{thing.size}}
        <br/>
        Цена: <strong>{{thing.cost}} руб.</strong>

    </ul>
</div>
<script src="/js/bootstrap.min.js"></script>
</body>
</html>
