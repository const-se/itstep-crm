<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta charset="UTF-8"/>
        <title>Заявка</title>
        <link rel="stylesheet" href="/css/bootstrap.min.css"/>
    </head>
    <body>
        <div class="container p-5">
            <h1>Заявка</h1>
            <form action="/save-order" method="POST" class="w-50 my-5">
                <div class="form-group">
                    <label for="comment">Логин</label>
                    <textarea id="comment" name="comment" class="form-control" placeholder="Комментарий"></textarea>
                </div>
                <div class="form-group">
                    <label for="car">Автомобиль</label>
                    <select id="car" name="car">
                        <option value="">-- Выберите автомобиль --</option>
                        {{#cars}}
                            <option value="{{id}}">{{brand.name}} {{name}}</option>
                        {{/cars}}
                    </select>
                </div>
                <button type="submit" class="btn btn-success">Оставить заявку</button>
            </form>
        </div>
        <script src="/js/bootstrap.min.js"></script>
    </body>
</html>